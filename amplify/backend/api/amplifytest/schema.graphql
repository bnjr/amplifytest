type Profile @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  phone: AWSPhone
  image: AWSURL
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}
 